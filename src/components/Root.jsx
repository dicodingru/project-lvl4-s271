import React from 'react';
import ChannelsContainer from '../containers/ChannelsContainer';

const Root = () => (
    <React.Fragment>
      <ChannelsContainer />
    </React.Fragment>
  );

export default Root;
